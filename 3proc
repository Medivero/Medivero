1.
#include <iostream>
#include <cmath>
using namespace std;
int main(){
    int S,p,n;
    cout << "S:";
    cin >> S;
    cout << "p:";
    cin >> p;
    cout << "n:";
    cin >> n;
    double r = p/100;
    cout << "Result(m) = " <<(S*r*pow(1+r,n))/(12*(pow(1+r,n)-1));
}
